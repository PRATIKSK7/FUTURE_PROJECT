/*
Type Definitions
Shared application interfaces for UI development.
Belongs in apps/web/types/
*/
export interface Project {
  id: string;
  name: string;
  location: string;
  usp: string;
  createdAt: string;
}
